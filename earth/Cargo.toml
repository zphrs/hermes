[package]
name = "earth"
version = "0.1.0"
edition = "2024"

[dependencies]
max-sized-vec = { version = "0.1.0", path = "../max-sized-vec" }
shared-schema = { version = "0.1.0", path = "../shared-schema" }
